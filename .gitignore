# Logs y capturas de herramientas locales (servidor de prueba, Playwright).
*.log
.playwright-mcp/

# Dependencias y basura del sistema.
node_modules/
.DS_Store
Thumbs.db

# App nativa (Capacitor): la carpeta www/ se arma con `npm run web` y lo compilado no se sube.
www/
android/app/build/
android/.gradle/
android/build/
android/local.properties
android/capacitor-cordova-android-plugins/build/
# Llaves de firma: NUNCA al repo.
*.keystore
*.jks
# Claves de Apple, certificados y variables de entorno: tampoco.
*.p8
*.p12
*.pem
*.mobileprovision
.env
.env.*
